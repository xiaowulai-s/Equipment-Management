# 2026-03-23 日志

## PySide6 + QML 界面修复

### 问题
1. QML组件中错误的import语句 `import "../Theme.qml"` 导致"no such directory"错误
2. TrendChart组件border.width重复定义
3. ModbusTable组件border.bottom无效属性
4. MainView.qml引用不存在的ThemeManager/Toast/ModalDialog组件

### 修复内容
1. 移除所有QML组件中的`import "../Theme.qml"`语句
2. 将Theme颜色硬编码到各组件中（颜色常量）
3. 修复TrendChart的border.width重复问题
4. 修复ModbusTable的border.bottom为独立的Rectangle
5. 重写MainView.qml，移除不存在的组件引用
6. 使用Python端的Theme对象通过engine.rootContext().setContextProperty()注入

### 当前状态
- 界面可正常启动
- 深色主题生效
- 数据每2秒更新一次
- 包含:侧边栏、趋势图、仪表盘、数据卡片、Modbus表格、设备列表

## UI组件设计规范修复

### 修复内容
按照UI设计方案.md和UI设计预览.html的要求，逐一修复了以下组件：

1. **Button.qml**
   - 添加图标按钮支持（isIconButton属性）
   - 添加iconSource属性支持SVG风格图标（add/edit/delete/settings/chart）
   - 完善悬停效果和阴影

2. **DataCard.qml**
   - 完善悬停动画效果（translateY: -4px）
   - 添加悬停阴影效果
   - 使用Canvas绘制趋势箭头图标替代文本符号
   - 添加发光效果到状态指示灯

3. **Gauge.qml**
   - 添加外发光效果层
   - 增强Canvas绘制的渐变和发光效果
   - 优化状态颜色变化

4. **ModbusTable.qml**
   - 添加排序指示器（Canvas绘制箭头）
   - 表头支持点击排序功能
   - 状态点添加脉冲动画效果
   - 优化分页按钮为Canvas绘制

5. **TrendChart.qml**
   - 时间范围按钮使用Canvas绘制SVG风格图标（时钟图标）
   - 保持原有功能完整

6. **DeviceList.qml**
   - 实现分组展开/折叠功能
   - 添加expandGroups数组跟踪展开状态
   - 展开/折叠图标使用Canvas绘制
   - 添加警告设备导航徽章

7. **MainView.qml**
   - 使用Canvas绘制所有导航图标（dashboard/monitor/settings/chart/config）
   - 使用Canvas绘制Logo图标
   - 使用Button组件替换主题切换按钮
   - 集成所有修复后的组件
   - 添加activeNavIndex跟踪激活状态

### 设计规范符合度
- 主题色: #2196F3 (科技蓝) ✓
- 辅助色: #00BCD4 (青色) ✓
- 状态色: 成功#4CAF50, 警告#FFC107, 错误#F44336 ✓
- 深色主题背景: #0F1419, #161B22, #1C2128 ✓
- 悬停效果: translateY(-4px) + shadow ✓
- SVG风格图标: 使用Canvas绘制 ✓
- Sticky表头: 在ModbusTable中实现 ✓
- 排序指示器: 在ModbusTable中实现 ✓

## 布局和显示优化 (2026-03-23 13:04)

### 修复内容
1. **侧边栏宽度**: 260px → 240px
2. **修复组件重叠**:
   - DeviceList改用`anchors.fill: parent`的clip容器
   - 主内容区列宽重新计算，避免溢出
3. **全局文字加粗**: 所有Text元素添加`font.bold: true`
4. **导航图标和文字尺寸微调**: 更紧凑的布局
5. **Logo标题简化**: "工业设备管理" → "设备管理"
6. **间距优化**: 整体spacing和margin调整